* Graceful handling if workers fail to start.
* test loader
* hook to add code before starting a worker
* Make setting spec_apts and deep_test at the same time more intuitive
* add failure method to Spec::WorkUnit::ResultReporter for when
   a before(:all) fails
* raise error when before/after all are defined
* Take care of timing issue in ProcessOrchestrator
