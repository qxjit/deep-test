* Graceful handling if workers fail to start.
* test loader
* Handle old versions of RSpec more gracefully
* Be more intelligent about finding example location based on backtrace
   - custom methods that call it() fail on the second invocation
* Log TupleSpace write error
* change befare(:all)/after(:all) to warn
* change tuplespace discovery based to include localhost again
